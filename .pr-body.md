## Summary
Brings the project to Level 4 (Excellent) on all rubric criteria with comprehensive test coverage, bug fixes, documentation, and CI improvements.

**Test coverage: 98.84% (595/602 lines) — 163 tests, 304 assertions, 0 failures**

### Changes

**Testing (Criterion 3):**
- Added SimpleCov for automated coverage reporting
- Added 92 new tests across models, services, controllers, and jobs
- Test cases TC-23 through TC-26 implemented
- Full integration tests for all 11 controllers
- Coverage interpretation documented in Deliverable3/README.md

**Bug fixes (Criterion 1):**
- `MaintenanceTicketsController#create` now accepts `lease_id` param for multi-store tenants (FR-16)
- `authorize_roles!` raises `UnauthorizedError` instead of rendering (prevents DoubleRenderError)
- `authorize_lease_access!` / `authorize_invoice_access!` properly halt controller execution
- Wired `ExpiredToken` and `UnauthorizedError` into BaseController rescue_from

**CI (Criterion 5):**
- Added `test` job to CI workflow with PostgreSQL service, migration, and test execution
- SimpleCov report uploaded as artifact

**Documentation (Criteria 2, 4, 5, 6):**
- README: Fixed version mismatch (React 19, React Router v7)
- README: Added User Story Traceability table (FR-01 through NFR-10)
- README: Added Known Scope Deferrals (NotificationService stub, utility simulation)
- Created Deliverable3/README.md with full repository guide and coverage interpretation

## Test plan
- [x] `rails test` — 163 tests pass, 0 failures, 0 errors
- [x] SimpleCov reports 98.84% line coverage
- [x] All controller endpoints return correct HTTP status codes
- [x] Multi-lease tenant maintenance ticket creation works with lease_id
